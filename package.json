{
  "name": "stampexpert",
  "version": "1.0.0",
  "description": "StampExpert static site",
  "scripts": {
    "start": "node server.js",
    "build": "node build.js",
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "jest": "^30.0.4",
    "live-server": "^1.2.1",
    "supertest": "^7.1.3"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}